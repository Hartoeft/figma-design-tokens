{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: get-design-tokens",
            "request": "launch",
            "command": "npm run get-design-tokens",
            "cwd": "${workspaceFolder}"
        }
    ]
}